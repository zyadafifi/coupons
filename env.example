# ==============================================================================
# ENVIRONMENT VARIABLES TEMPLATE
# ==============================================================================
# Copy this file to .env.local for local development
# Configure these in Netlify/Vercel dashboard for production
# 
# SECURITY WARNING: Never commit actual .env files with real values!
# ==============================================================================

# ------------------------------------------------------------------------------
# FIREBASE CONFIGURATION (Client-side - safe to expose)
# ------------------------------------------------------------------------------
# Get these from Firebase Console > Project Settings > General > Your apps
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=

# ------------------------------------------------------------------------------
# SUPABASE CONFIGURATION (Client-side - safe to expose)
# ------------------------------------------------------------------------------
# Get these from Supabase Dashboard > Settings > API
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# ------------------------------------------------------------------------------
# ADMIN PANEL CONFIGURATION
# ------------------------------------------------------------------------------
# CRITICAL: Set to "true" for web builds (Netlify), "false" for mobile builds
# Web (Netlify): VITE_ENABLE_ADMIN=true
# Mobile (Capacitor): VITE_ENABLE_ADMIN=false
VITE_ENABLE_ADMIN=false

# ------------------------------------------------------------------------------
# BUILD CONFIGURATION
# ------------------------------------------------------------------------------
NODE_VERSION=18
NODE_ENV=production

# ==============================================================================
# SERVER-SIDE ONLY VARIABLES (DO NOT PREFIX WITH VITE_)
# ==============================================================================
# These should ONLY be set in:
# - Netlify Functions environment variables
# - Supabase Edge Functions secrets
# - Never in client-side code or .env files
# ==============================================================================

# FIREBASE SERVICE ACCOUNT (Server-side only - Supabase Edge Functions)
# Full JSON of service account key - set this in Supabase dashboard secrets
# FIREBASE_SERVICE_ACCOUNT={"type":"service_account",...}

# ADMIN UID ALLOWLIST (Server-side only - Supabase Edge Functions)
# Comma-separated list of Firebase user UIDs with admin access
# Example: ADMIN_UID_ALLOWLIST=uid1,uid2,uid3
# ADMIN_UID_ALLOWLIST=

# SUPABASE SERVICE ROLE KEY (Server-side only)
# NEVER expose this in client-side code
# SUPABASE_SERVICE_ROLE_KEY=

# ==============================================================================
# DEPLOYMENT NOTES
# ==============================================================================
# 
# LOCAL DEVELOPMENT:
# 1. Copy this file to .env.local
# 2. Fill in your Firebase config values
# 3. Set VITE_ENABLE_ADMIN=true for testing admin features
# 
# NETLIFY DEPLOYMENT (Web Admin):
# 1. Go to Netlify Dashboard > Site Settings > Environment Variables
# 2. Add all VITE_* variables with production values
# 3. Set VITE_ENABLE_ADMIN=true (enables admin panel)
# 
# MOBILE BUILD:
# 1. Ensure VITE_ENABLE_ADMIN=false in build command
# 2. Use: npm run build:mobile
# 
# SUPABASE EDGE FUNCTIONS:
# 1. Set secrets via Supabase CLI or dashboard
# 2. Required: FIREBASE_SERVICE_ACCOUNT (full JSON)
# 3. Optional: ADMIN_UID_ALLOWLIST (if not using custom claims)
# 
# ==============================================================================

